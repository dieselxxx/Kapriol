@startuml

package HTTP <<Node>> #FBE9E7 {
    namespace FireHub\Jezgra\HTTP {
        class Kernel #00897B;line:00796B {
            - Ruter $ruter
            - Zahtjev $zahtjev
            + __construct (private Zahtjev $zahtjev):void
            + pokreni ():HTTP_Odgovor
            - postaviAplikaciju ():self
            - trenutnaAplikacija ():string
            - ruter ():self
            - odgovor ():HTTP_Odgovor
        }
    }
}

@enduml